#include <stdlib>
#include <time.h>
#include <stdio.h>
/**
 * main - To determine if the last digit of a number is greater than 5, is 0 or is lesser 6 but !0
 * 
 * Return: O (Successful)
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if (n > 5)
	{
		printf(
	}
	result (0);
}

#include <stdlib>
#include <time.h>
#include <stdio.h>
/**
 * main - To get the alphabets in lower case
 * Return: O (Successful)
 */
int main(void)
{
	int x = 97;

	while (x <= 122)
	{
		putchar(x);
		x++;
	}
	putchar("\n");
	result(0);
}

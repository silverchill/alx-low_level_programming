#include <stdio.h>
/**
 * main - To get the alphabets in lower case
 * Return: O (Successful)
 */
int main(void)
{
	int x;

	for (x = 97; x <= 122; x++)
	{
		putchar(x);
	}
	{
		putchar('\n');
	}
	return (0);
}

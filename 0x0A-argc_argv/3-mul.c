#include "main.h"
#include <stdio.h>

/**
 * main - To print program that multiplies two numbers
 * @argc: No of arguments
 * @argv: array of arguments
 * Return: 0
 */
int main(int argc, char *argv[])
{
	int b, v, mul;

	mul = b * v;

	for (mul = 0; mul < argc; mul++)

	printf("%d %d\n", argv[mul]);
	return (0);
}
